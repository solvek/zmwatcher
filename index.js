module.exports = {
    /*
    Watches for one or more cameras changes
    parameters:
    eventsPath - the path to events directory
    cam1, cam2, cam3... - numbers of cameras to watch
     */
    watch: function(eventsPath){

    }
}